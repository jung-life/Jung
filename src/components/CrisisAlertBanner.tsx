// Crisis Alert Banner Component for Jung Therapeutic AI\n// Shows persistent crisis support information during conversations\n\nimport React from 'react';\nimport { View, Text, TouchableOpacity, Linking } from 'react-native';\nimport tw from 'twrnc';\nimport { Ionicons } from '@expo/vector-icons';\n\nexport interface CrisisAlertBannerProps {\n  severity: 'low' | 'medium' | 'high';\n  onShowResources: () => void;\n  onDismiss?: () => void;\n  compact?: boolean;\n}\n\nexport const CrisisAlertBanner: React.FC<CrisisAlertBannerProps> = ({\n  severity,\n  onShowResources,\n  onDismiss,\n  compact = false\n}) => {\n  const getConfig = () => {\n    switch (severity) {\n      case 'high':\n        return {\n          backgroundColor: '#FEF2F2', // Red-50\n          borderColor: '#F87171', // Red-400\n          textColor: '#7F1D1D', // Red-900\n          iconColor: '#DC2626', // Red-600\n          title: 'Crisis Support Available',\n          message: 'Immediate help is available 24/7',\n          actionText: 'Get Help Now',\n          showCallButton: true\n        };\n      case 'medium':\n        return {\n          backgroundColor: '#FEF3C7', // Yellow-100\n          borderColor: '#F59E0B', // Yellow-500\n          textColor: '#78350F', // Yellow-900\n          iconColor: '#D97706', // Yellow-600\n          title: 'Support Resources',\n          message: 'Help is available if you need it',\n          actionText: 'View Resources',\n          showCallButton: false\n        };\n      case 'low':\n      default:\n        return {\n          backgroundColor: '#EBF8FF', // Blue-50\n          borderColor: '#60A5FA', // Blue-400\n          textColor: '#1E3A8A', // Blue-900\n          iconColor: '#3B82F6', // Blue-500\n          title: 'Additional Support',\n          message: 'Resources are available 24/7',\n          actionText: 'Learn More',\n          showCallButton: false\n        };\n    }\n  };\n\n  const config = getConfig();\n\n  const handleQuickCall = async () => {\n    try {\n      const supported = await Linking.canOpenURL('tel:988');\n      if (supported) {\n        await Linking.openURL('tel:988');\n      }\n    } catch (error) {\n      console.error('Error opening phone dialer:', error);\n    }\n  };\n\n  if (compact) {\n    return (\n      <View style={[\n        tw`mx-4 mb-3 rounded-lg border p-3 flex-row items-center`,\n        { backgroundColor: config.backgroundColor, borderColor: config.borderColor }\n      ]}>\n        <Ionicons \n          name=\"information-circle\" \n          size={20} \n          color={config.iconColor} \n          style={tw`mr-3`}\n        />\n        <View style={tw`flex-1`}>\n          <Text style={[tw`font-medium text-sm`, { color: config.textColor }]}>\n            {config.title}\n          </Text>\n        </View>\n        <TouchableOpacity\n          onPress={onShowResources}\n          style={tw`ml-3`}\n        >\n          <Text style={[tw`text-sm font-medium underline`, { color: config.iconColor }]}>\n            {config.actionText}\n          </Text>\n        </TouchableOpacity>\n        {onDismiss && (\n          <TouchableOpacity\n            onPress={onDismiss}\n            style={tw`ml-2 p-1`}\n          >\n            <Ionicons name=\"close\" size={16} color={config.textColor} />\n          </TouchableOpacity>\n        )}\n      </View>\n    );\n  }\n\n  return (\n    <View style={[\n      tw`mx-4 mb-4 rounded-xl border p-4`,\n      { backgroundColor: config.backgroundColor, borderColor: config.borderColor }\n    ]}>\n      <View style={tw`flex-row items-start`}>\n        <View style={[\n          tw`w-10 h-10 rounded-full items-center justify-center mr-3 mt-1`,\n          { backgroundColor: config.borderColor + '20' }\n        ]}>\n          <Ionicons \n            name={severity === 'high' ? 'medical' : 'information-circle'} \n            size={20} \n            color={config.iconColor} \n          />\n        </View>\n        \n        <View style={tw`flex-1`}>\n          <Text style={[tw`font-semibold text-base mb-1`, { color: config.textColor }]}>\n            {config.title}\n          </Text>\n          <Text style={[tw`text-sm mb-3`, { color: config.textColor }]}>\n            {config.message}\n          </Text>\n          \n          <View style={tw`flex-row items-center space-x-3`}>\n            {config.showCallButton && (\n              <TouchableOpacity\n                onPress={handleQuickCall}\n                style={[\n                  tw`px-4 py-2 rounded-lg flex-row items-center`,\n                  { backgroundColor: config.iconColor }\n                ]}\n              >\n                <Ionicons name=\"call\" size={16} color=\"white\" style={tw`mr-2`} />\n                <Text style={tw`text-white font-medium text-sm`}>\n                  Call 988\n                </Text>\n              </TouchableOpacity>\n            )}\n            \n            <TouchableOpacity\n              onPress={onShowResources}\n              style={[\n                tw`px-4 py-2 rounded-lg border`,\n                { \n                  borderColor: config.iconColor, \n                  backgroundColor: config.showCallButton ? 'transparent' : config.iconColor \n                }\n              ]}\n            >\n              <Text style={[\n                tw`font-medium text-sm`,\n                { color: config.showCallButton ? config.iconColor : 'white' }\n              ]}>\n                {config.actionText}\n              </Text>\n            </TouchableOpacity>\n          </View>\n        </View>\n        \n        {onDismiss && (\n          <TouchableOpacity\n            onPress={onDismiss}\n            style={tw`p-2 -mt-2 -mr-2`}\n          >\n            <Ionicons name=\"close\" size={20} color={config.textColor} />\n          </TouchableOpacity>\n        )}\n      </View>\n      \n      {/* Quick Facts */}\n      {severity === 'high' && (\n        <View style={tw`mt-4 pt-4 border-t border-gray-200`}>\n          <Text style={[tw`text-xs font-medium mb-2`, { color: config.textColor }]}>\n            IMMEDIATE RESOURCES:\n          </Text>\n          <View style={tw`space-y-1`}>\n            <Text style={[tw`text-xs`, { color: config.textColor }]}>\n              • Crisis Lifeline: 988 (call or chat)\n            </Text>\n            <Text style={[tw`text-xs`, { color: config.textColor }]}>\n              • Crisis Text: Text HOME to 741741\n            </Text>\n            <Text style={[tw`text-xs`, { color: config.textColor }]}>\n              • Emergency: 911\n            </Text>\n          </View>\n        </View>\n      )}\n    </View>\n  );\n};\n\n// Crisis Message Component\nexport interface CrisisMessageProps {\n  severity: 'low' | 'medium' | 'high';\n  message: string;\n  onShowResources: () => void;\n}\n\nexport const CrisisMessage: React.FC<CrisisMessageProps> = ({\n  severity,\n  message,\n  onShowResources\n}) => {\n  const getBorderColor = () => {\n    switch (severity) {\n      case 'high': return '#F87171';\n      case 'medium': return '#F59E0B';\n      default: return '#60A5FA';\n    }\n  };\n\n  const getBackgroundColor = () => {\n    switch (severity) {\n      case 'high': return '#FEF2F2';\n      case 'medium': return '#FEF3C7';\n      default: return '#EBF8FF';\n    }\n  };\n\n  return (\n    <View style={[\n      tw`mx-4 my-2 rounded-xl border-l-4 p-4`,\n      { \n        borderLeftColor: getBorderColor(),\n        backgroundColor: getBackgroundColor()\n      }\n    ]}>\n      <Text style={tw`text-gray-800 text-sm leading-5 mb-3`}>\n        {message}\n      </Text>\n      \n      <TouchableOpacity\n        onPress={onShowResources}\n        style={[\n          tw`self-start px-3 py-1 rounded-md`,\n          { backgroundColor: getBorderColor() }\n        ]}\n      >\n        <Text style={tw`text-white text-xs font-medium`}>\n          View Support Resources\n        </Text>\n      </TouchableOpacity>\n    </View>\n  );\n};"