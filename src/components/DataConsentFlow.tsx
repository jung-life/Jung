// Data Consent Flow for Jung Therapeutic AI\n// HIPAA-compliant consent collection for pattern analysis\n\nimport React, { useState } from 'react';\nimport {\n  View,\n  Text,\n  TouchableOpacity,\n  ScrollView,\n  Modal,\n  Alert\n} from 'react-native';\nimport tw from 'twrnc';\nimport { Ionicons } from '@expo/vector-icons';\nimport AsyncStorage from '@react-native-async-storage/async-storage';\n\nexport interface ConsentData {\n  patternAnalysis: boolean;\n  dataRetention: number; // days\n  anonymizedResearch: boolean;\n  usageAnalytics: boolean;\n  timestamp: number;\n  version: string;\n}\n\nexport interface DataConsentFlowProps {\n  visible: boolean;\n  onComplete: (consent: ConsentData) => void;\n  onSkip?: () => void;\n}\n\nconst CONSENT_VERSION = '1.0.0';\nconst CONSENT_STORAGE_KEY = '@jung_data_consent';\n\nexport const DataConsentFlow: React.FC<DataConsentFlowProps> = ({\n  visible,\n  onComplete,\n  onSkip\n}) => {\n  const [currentStep, setCurrentStep] = useState(0);\n  const [consent, setConsent] = useState<Partial<ConsentData>>({\n    patternAnalysis: true,\n    dataRetention: 90,\n    anonymizedResearch: false,\n    usageAnalytics: false\n  });\n\n  const steps = [\n    {\n      title: \"Welcome to Jung\",\n      subtitle: \"Let's set up your privacy preferences\",\n      icon: \"shield-checkmark\",\n      content: (\n        <View>\n          <Text style={tw`text-gray-700 text-base leading-6 mb-4`}>\n            Jung is designed with your privacy as the top priority. Before we begin, let's configure how your data is handled.\n          </Text>\n          <View style={tw`bg-blue-50 rounded-lg p-4`}>\n            <Text style={tw`text-blue-900 font-semibold mb-2`}>Your Data is Always:</Text>\n            <View style={tw`space-y-1`}>\n              <Text style={tw`text-blue-800 text-sm`}>• Encrypted end-to-end</Text>\n              <Text style={tw`text-blue-800 text-sm`}>• Never sold or shared</Text>\n              <Text style={tw`text-blue-800 text-sm`}>• Under your complete control</Text>\n              <Text style={tw`text-blue-800 text-sm`}>• HIPAA & GDPR compliant</Text>\n            </View>\n          </View>\n        </View>\n      )\n    },\n    {\n      title: \"Therapeutic Pattern Analysis\",\n      subtitle: \"Help Jung understand your needs better\",\n      icon: \"analytics\",\n      content: (\n        <View>\n          <Text style={tw`text-gray-700 text-base leading-6 mb-4`}>\n            Jung can analyze patterns in your conversations to provide more personalized therapeutic support. This helps identify connections between different concerns (like stress and sleep issues).\n          </Text>\n          \n          <View style={tw`bg-green-50 rounded-lg p-4 mb-4`}>\n            <Text style={tw`text-green-900 font-semibold mb-2`}>How Pattern Analysis Works:</Text>\n            <View style={tw`space-y-1`}>\n              <Text style={tw`text-green-800 text-sm`}>• Your conversations are encrypted and anonymized</Text>\n              <Text style={tw`text-green-800 text-sm`}>• Only emotional themes are extracted (anxiety, depression, etc.)</Text>\n              <Text style={tw`text-green-800 text-sm`}>• Personal details are automatically removed</Text>\n              <Text style={tw`text-green-800 text-sm`}>• Patterns help Jung provide contextual support</Text>\n            </View>\n          </View>\n\n          <TouchableOpacity\n            onPress={() => setConsent({ ...consent, patternAnalysis: !consent.patternAnalysis })}\n            style={tw`flex-row items-center p-4 border rounded-lg ${\n              consent.patternAnalysis ? 'bg-blue-50 border-blue-200' : 'bg-gray-50 border-gray-200'\n            }`}\n          >\n            <View style={[\n              tw`w-6 h-6 rounded border-2 mr-3 items-center justify-center`,\n              consent.patternAnalysis ? tw`bg-blue-600 border-blue-600` : tw`border-gray-400`\n            ]}>\n              {consent.patternAnalysis && (\n                <Ionicons name=\"checkmark\" size={16} color=\"white\" />\n              )}\n            </View>\n            <View style={tw`flex-1`}>\n              <Text style={tw`font-medium text-gray-900`}>Enable Pattern Analysis</Text>\n              <Text style={tw`text-sm text-gray-600`}>Recommended for better therapeutic support</Text>\n            </View>\n          </TouchableOpacity>\n\n          {!consent.patternAnalysis && (\n            <View style={tw`mt-4 p-3 bg-yellow-50 rounded-lg border border-yellow-200`}>\n              <Text style={tw`text-yellow-800 text-sm`}>\n                ⚠️ Without pattern analysis, Jung won't be able to provide contextual therapeutic insights or remember themes across conversations.\n              </Text>\n            </View>\n          )}\n        </View>\n      )\n    },\n    {\n      title: \"Data Retention\",\n      subtitle: \"How long should we keep your conversations?\",\n      icon: \"time\",\n      content: (\n        <View>\n          <Text style={tw`text-gray-700 text-base leading-6 mb-4`}>\n            Choose how long Jung should retain your conversation data. Longer retention allows for better long-term pattern recognition and progress tracking.\n          </Text>\n          \n          <View style={tw`space-y-3`}>\n            {[\n              { days: 30, label: '30 Days', description: 'Short-term support only' },\n              { days: 90, label: '3 Months', description: 'Good for pattern recognition (Recommended)' },\n              { days: 180, label: '6 Months', description: 'Better long-term insights' },\n              { days: 365, label: '1 Year', description: 'Complete therapeutic journey tracking' }\n            ].map((option) => (\n              <TouchableOpacity\n                key={option.days}\n                onPress={() => setConsent({ ...consent, dataRetention: option.days })}\n                style={tw`p-4 border rounded-lg flex-row items-center ${\n                  consent.dataRetention === option.days \n                    ? 'bg-blue-50 border-blue-200' \n                    : 'bg-gray-50 border-gray-200'\n                }`}\n              >\n                <View style={[\n                  tw`w-6 h-6 rounded-full border-2 mr-3`,\n                  consent.dataRetention === option.days \n                    ? tw`bg-blue-600 border-blue-600`\n                    : tw`border-gray-400`\n                ]}>\n                  {consent.dataRetention === option.days && (\n                    <View style={tw`w-2 h-2 bg-white rounded-full self-center mt-0.5`} />\n                  )}\n                </View>\n                <View style={tw`flex-1`}>\n                  <Text style={tw`font-medium text-gray-900`}>{option.label}</Text>\n                  <Text style={tw`text-sm text-gray-600`}>{option.description}</Text>\n                </View>\n                {option.days === 90 && (\n                  <View style={tw`px-2 py-1 bg-blue-100 rounded`}>\n                    <Text style={tw`text-xs text-blue-700 font-medium`}>Recommended</Text>\n                  </View>\n                )}\n              </TouchableOpacity>\n            ))}\n          </View>\n          \n          <View style={tw`mt-4 p-3 bg-gray-50 rounded-lg`}>\n            <Text style={tw`text-gray-700 text-sm`}>\n              💡 You can always export your data or delete it entirely from Privacy Settings.\n            </Text>\n          </View>\n        </View>\n      )\n    },\n    {\n      title: \"Optional Features\",\n      subtitle: \"Help improve Jung while maintaining privacy\",\n      icon: \"settings\",\n      content: (\n        <View>\n          <Text style={tw`text-gray-700 text-base leading-6 mb-4`}>\n            These optional features help improve Jung for everyone while maintaining your complete privacy.\n          </Text>\n          \n          <View style={tw`space-y-4`}>\n            {/* Anonymized Research */}\n            <TouchableOpacity\n              onPress={() => setConsent({ ...consent, anonymizedResearch: !consent.anonymizedResearch })}\n              style={tw`p-4 border rounded-lg ${\n                consent.anonymizedResearch ? 'bg-blue-50 border-blue-200' : 'bg-gray-50 border-gray-200'\n              }`}\n            >\n              <View style={tw`flex-row items-start`}>\n                <View style={[\n                  tw`w-6 h-6 rounded border-2 mr-3 items-center justify-center mt-0.5`,\n                  consent.anonymizedResearch ? tw`bg-blue-600 border-blue-600` : tw`border-gray-400`\n                ]}>\n                  {consent.anonymizedResearch && (\n                    <Ionicons name=\"checkmark\" size={16} color=\"white\" />\n                  )}\n                </View>\n                <View style={tw`flex-1`}>\n                  <Text style={tw`font-medium text-gray-900 mb-1`}>Anonymized Research</Text>\n                  <Text style={tw`text-sm text-gray-600 leading-5`}>\n                    Help improve therapeutic AI by contributing anonymized, aggregated patterns. No personal information or conversation content is shared.\n                  </Text>\n                </View>\n              </View>\n            </TouchableOpacity>\n\n            {/* Usage Analytics */}\n            <TouchableOpacity\n              onPress={() => setConsent({ ...consent, usageAnalytics: !consent.usageAnalytics })}\n              style={tw`p-4 border rounded-lg ${\n                consent.usageAnalytics ? 'bg-blue-50 border-blue-200' : 'bg-gray-50 border-gray-200'\n              }`}\n            >\n              <View style={tw`flex-row items-start`}>\n                <View style={[\n                  tw`w-6 h-6 rounded border-2 mr-3 items-center justify-center mt-0.5`,\n                  consent.usageAnalytics ? tw`bg-blue-600 border-blue-600` : tw`border-gray-400`\n                ]}>\n                  {consent.usageAnalytics && (\n                    <Ionicons name=\"checkmark\" size={16} color=\"white\" />\n                  )}\n                </View>\n                <View style={tw`flex-1`}>\n                  <Text style={tw`font-medium text-gray-900 mb-1`}>Usage Analytics</Text>\n                  <Text style={tw`text-sm text-gray-600 leading-5`}>\n                    Share anonymous app usage statistics to help us improve performance and features. No conversation content is included.\n                  </Text>\n                </View>\n              </View>\n            </TouchableOpacity>\n          </View>\n          \n          <View style={tw`mt-4 p-3 bg-green-50 rounded-lg border border-green-200`}>\n            <Text style={tw`text-green-800 text-sm`}>\n              ✅ Both features are completely optional and can be changed anytime in Privacy Settings.\n            </Text>\n          </View>\n        </View>\n      )\n    },\n    {\n      title: \"Review Your Choices\",\n      subtitle: \"Confirm your privacy preferences\",\n      icon: \"checkmark-circle\",\n      content: (\n        <View>\n          <Text style={tw`text-gray-700 text-base leading-6 mb-6`}>\n            Here's a summary of your privacy preferences. You can change these anytime in Settings.\n          </Text>\n          \n          <View style={tw`bg-white rounded-lg border border-gray-200 p-4 space-y-4`}>\n            <View style={tw`flex-row items-center justify-between`}>\n              <Text style={tw`font-medium text-gray-900`}>Pattern Analysis</Text>\n              <View style={tw`flex-row items-center`}>\n                <Text style={[\n                  tw`text-sm mr-2`,\n                  consent.patternAnalysis ? tw`text-green-600` : tw`text-red-600`\n                ]}>\n                  {consent.patternAnalysis ? 'Enabled' : 'Disabled'}\n                </Text>\n                <Ionicons \n                  name={consent.patternAnalysis ? \"checkmark-circle\" : \"close-circle\"} \n                  size={20} \n                  color={consent.patternAnalysis ? \"#10B981\" : \"#EF4444\"} \n                />\n              </View>\n            </View>\n            \n            <View style={tw`flex-row items-center justify-between`}>\n              <Text style={tw`font-medium text-gray-900`}>Data Retention</Text>\n              <Text style={tw`text-sm text-gray-600`}>\n                {consent.dataRetention === 365 ? '1 year' : `${consent.dataRetention} days`}\n              </Text>\n            </View>\n            \n            <View style={tw`flex-row items-center justify-between`}>\n              <Text style={tw`font-medium text-gray-900`}>Anonymized Research</Text>\n              <View style={tw`flex-row items-center`}>\n                <Text style={[\n                  tw`text-sm mr-2`,\n                  consent.anonymizedResearch ? tw`text-green-600` : tw`text-gray-600`\n                ]}>\n                  {consent.anonymizedResearch ? 'Enabled' : 'Disabled'}\n                </Text>\n                <Ionicons \n                  name={consent.anonymizedResearch ? \"checkmark-circle\" : \"close-circle\"} \n                  size={20} \n                  color={consent.anonymizedResearch ? \"#10B981\" : \"#9CA3AF\"} \n                />\n              </View>\n            </View>\n            \n            <View style={tw`flex-row items-center justify-between`}>\n              <Text style={tw`font-medium text-gray-900`}>Usage Analytics</Text>\n              <View style={tw`flex-row items-center`}>\n                <Text style={[\n                  tw`text-sm mr-2`,\n                  consent.usageAnalytics ? tw`text-green-600` : tw`text-gray-600`\n                ]}>\n                  {consent.usageAnalytics ? 'Enabled' : 'Disabled'}\n                </Text>\n                <Ionicons \n                  name={consent.usageAnalytics ? \"checkmark-circle\" : \"close-circle\"} \n                  size={20} \n                  color={consent.usageAnalytics ? \"#10B981\" : \"#9CA3AF\"} \n                />\n              </View>\n            </View>\n          </View>\n          \n          <View style={tw`mt-6 p-4 bg-blue-50 rounded-lg border border-blue-200`}>\n            <View style={tw`flex-row items-start`}>\n              <Ionicons name=\"information-circle\" size={20} color=\"#3B82F6\" style={tw`mr-2 mt-0.5`} />\n              <Text style={tw`text-blue-800 text-sm leading-5 flex-1`}>\n                These settings comply with HIPAA and GDPR requirements. Your conversations are always encrypted and you have complete control over your data.\n              </Text>\n            </View>\n          </View>\n        </View>\n      )\n    }\n  ];\n\n  const currentStepData = steps[currentStep];\n  const isLastStep = currentStep === steps.length - 1;\n\n  const handleNext = () => {\n    if (isLastStep) {\n      handleComplete();\n    } else {\n      setCurrentStep(currentStep + 1);\n    }\n  };\n\n  const handleBack = () => {\n    if (currentStep > 0) {\n      setCurrentStep(currentStep - 1);\n    }\n  };\n\n  const handleComplete = async () => {\n    const finalConsent: ConsentData = {\n      patternAnalysis: consent.patternAnalysis ?? true,\n      dataRetention: consent.dataRetention ?? 90,\n      anonymizedResearch: consent.anonymizedResearch ?? false,\n      usageAnalytics: consent.usageAnalytics ?? false,\n      timestamp: Date.now(),\n      version: CONSENT_VERSION\n    };\n\n    try {\n      await AsyncStorage.setItem(CONSENT_STORAGE_KEY, JSON.stringify(finalConsent));\n      onComplete(finalConsent);\n    } catch (error) {\n      console.error('Error saving consent:', error);\n      Alert.alert('Error', 'Failed to save privacy preferences');\n    }\n  };\n\n  const handleSkip = () => {\n    if (onSkip) {\n      onSkip();\n    }\n  };\n\n  return (\n    <Modal\n      visible={visible}\n      animationType=\"slide\"\n      presentationStyle=\"pageSheet\"\n    >\n      <View style={tw`flex-1 bg-gray-50`}>\n        {/* Header */}\n        <View style={tw`bg-white px-6 pt-12 pb-6 border-b border-gray-200`}>\n          <View style={tw`flex-row items-center justify-between mb-6`}>\n            <View style={tw`flex-row items-center`}>\n              <View style={tw`w-12 h-12 bg-blue-100 rounded-full items-center justify-center mr-3`}>\n                <Ionicons name={currentStepData.icon as any} size={24} color=\"#3B82F6\" />\n              </View>\n              <View>\n                <Text style={tw`text-xl font-bold text-gray-900`}>\n                  {currentStepData.title}\n                </Text>\n                <Text style={tw`text-sm text-gray-600`}>\n                  {currentStepData.subtitle}\n                </Text>\n              </View>\n            </View>\n            {onSkip && currentStep === 0 && (\n              <TouchableOpacity onPress={handleSkip} style={tw`p-2`}>\n                <Text style={tw`text-gray-600 font-medium`}>Skip</Text>\n              </TouchableOpacity>\n            )}\n          </View>\n          \n          {/* Progress */}\n          <View style={tw`flex-row space-x-2`}>\n            {steps.map((_, index) => (\n              <View\n                key={index}\n                style={[\n                  tw`h-2 flex-1 rounded`,\n                  index <= currentStep ? tw`bg-blue-600` : tw`bg-gray-200`\n                ]}\n              />\n            ))}\n          </View>\n        </View>\n\n        {/* Content */}\n        <ScrollView style={tw`flex-1 px-6 py-6`}>\n          {currentStepData.content}\n        </ScrollView>\n\n        {/* Footer */}\n        <View style={tw`bg-white px-6 py-4 border-t border-gray-200`}>\n          <View style={tw`flex-row space-x-3`}>\n            {currentStep > 0 && (\n              <TouchableOpacity\n                onPress={handleBack}\n                style={tw`flex-1 py-3 px-4 rounded-xl border border-gray-300`}\n              >\n                <Text style={tw`text-gray-700 font-medium text-center`}>Back</Text>\n              </TouchableOpacity>\n            )}\n            <TouchableOpacity\n              onPress={handleNext}\n              style={tw`flex-1 py-3 px-4 bg-blue-600 rounded-xl`}\n            >\n              <Text style={tw`text-white font-semibold text-center`}>\n                {isLastStep ? 'Complete Setup' : 'Continue'}\n              </Text>\n            </TouchableOpacity>\n          </View>\n        </View>\n      </View>\n    </Modal>\n  );\n};\n\n// Helper function to check if consent is needed\nexport const checkConsentStatus = async (): Promise<ConsentData | null> => {\n  try {\n    const stored = await AsyncStorage.getItem(CONSENT_STORAGE_KEY);\n    if (stored) {\n      const consent: ConsentData = JSON.parse(stored);\n      // Check if consent is still valid (version matches)\n      if (consent.version === CONSENT_VERSION) {\n        return consent;\n      }\n    }\n    return null;\n  } catch (error) {\n    console.error('Error checking consent status:', error);\n    return null;\n  }\n};\n\n// Helper function to update existing consent\nexport const updateConsent = async (updates: Partial<ConsentData>): Promise<ConsentData | null> => {\n  try {\n    const existing = await checkConsentStatus();\n    if (existing) {\n      const updated = {\n        ...existing,\n        ...updates,\n        timestamp: Date.now()\n      };\n      await AsyncStorage.setItem(CONSENT_STORAGE_KEY, JSON.stringify(updated));\n      return updated;\n    }\n    return null;\n  } catch (error) {\n    console.error('Error updating consent:', error);\n    return null;\n  }\n};"